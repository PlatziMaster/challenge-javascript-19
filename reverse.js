const weather = ['🌤', '🌧']
const reversed = weather.reverse()

console.log(reversed)//--[ '🌧', '🌤' ]
console.log(weather)//--[ '🌧', '🌤' ]

//the reverse method invert the items in the array
// WARNING: it change the original array 